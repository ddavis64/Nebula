version: "3.8"

services:
  lighthouse:
    build: .
    container_name: nebula-lighthouse
    network_mode: host
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    volumes:
      - ../config:/config
      - ../certs:/certs
    ports:
      - "4242:4242/udp"
